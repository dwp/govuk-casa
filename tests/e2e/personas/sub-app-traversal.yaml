# Traverse forwards and backwards using ephemeral context

journey:
  start:
    subAppContextButton: click()
  sub-app/:contextid/meals:
    chilli: select()
  sub-app/:contextid/complete:
    # Continue
  country:
    # This is the start waypoint of the main fully-loaded journey
    # No interaction
