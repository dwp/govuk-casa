const doGather = require('./gather.js');
const doValidation = require('./validation.js');
const doRedirect = require('./redirect.js');
const doRender = require('./render.js');

module.exports = {
  doGather,
  doValidation,
  doRedirect,
  doRender,
};
